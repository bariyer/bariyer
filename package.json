{
  "name": "bariyer",
  "description": "bariyer is an authorization middleware library for express.js",
  "version": "0.1.0",
  "workspaces": {
    "packages": [
      "examples/*",
      "packages/*"
    ]
  }
}
