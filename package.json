{
  "name": "koauth",
  "description": "Koauth is an authorization middleware library for express.js",
  "version": "0.1.0",
  "workspaces": {
    "packages": [
      "examples/*",
      "packages/*"
    ]
  }
}
