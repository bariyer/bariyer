{
  "name": "@koauth/example-server",
  "version": "0.0.1",
  "scripts": {
    "basic:start": "node basic/",
    "basic:watch": "nodemon --watch basic/ basic/"
  },
  "dependencies": {
    "@koauth/server": "workspace:packages/server",
    "dotenv": "^10.0.0",
    "express": "^4.17.1"
  },
  "devDependencies": {
    "nodemon": "^2.0.11"
  }
}
